

const Title = ({ title }) => {
    
    return (
        <div>
            <h2 className='text-primary heading text-3xl p-3 lg:p-0 lg:text-5xl font-bold mt-18 text-center'>{ title}</h2>
        </div>
    );
};

export default Title;